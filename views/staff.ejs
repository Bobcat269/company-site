<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Staff</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            margin: 20px;
            background-color: #f4f4f9;
        }

        .staff-item {
            margin-bottom: 30px;
            padding: 15px;
            background: #fff;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        .staff-item h1 {
            font-size: 1.5rem;
            margin: 0 0 10px;
            color: #14b057; /* Logo color */
        }

        .staff-item p {
            margin: 0 0 10px;
            color: #555;
        }

        .staff-item ul {
            list-style-type: none;
            padding: 0;
            margin: 0;
        }

        .staff-item ul li {
            margin: 5px 0;
            padding: 10px;
            background: #00050a; /* Logo color scheme */
            color: #fff;
            border-radius: 5px;
            text-align: center;
        }

        .staff-item ul li a {
            color: #fff;
            text-decoration: none;
            display: block;
        }

        .staff-item ul li:hover {
            background: #0056b3;
        }
    </style>
</head>
<body>
    <%- include("partials/nav") %>

    <% staff.forEach((person, index) => { %>
        <div class="staff-item">
            <!-- Header for each staff member -->
            <h1><%= person.name %></h1>

            <!-- Staff details -->
            <p><%= person.role %></p>

            <!-- Link for staff member -->
            <ul>
                <li>
                    <a href="/staff/<%= index %>">More about <%= person.name %></a>
                </li>
            </ul>
        </div>
    <% }) %>
</body>
</html>
