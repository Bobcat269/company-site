<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>past-work</title>
</head>
<body>
    <%- include("partials/nav") %>
    <%works.forEach((work) => {%>
        <h1><%= work.name%></h1>
        <p><%= work.description%></p>
    <%})%>

    <ul>
        <% works.forEach((work, index)  => { %>
            <!-- Wrap each li in an a tag -->
            <a href= "/past-work/<%=index%>">
                <li><%= work.name %></li>

            </a>
         
        <% }); %>
       
    </ul>
</body>
</html>